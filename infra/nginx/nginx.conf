events {
    worker_connections 1024;
}

stream {
    upstream postgres_backend {
        server postgres:5432;
    }

    server {
        listen 5432;
        proxy_pass postgres_backend;
        proxy_timeout 10s;
        proxy_responses 1;
    }
}
